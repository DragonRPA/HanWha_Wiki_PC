# 릴리즈 노트 (RELEASE NOTES)

## v1.5.1.Build.1 (2026-08-07 14:57)

### 🐛 사이드바-드롭다운 필터 일원화 & 불필요 교집합(AND 조건) 오작동 픽스
- **원인 분석**:
  - 기존 코드에서 사이드바 카테고리(`sidebarCategory`)와 상단 드롭다운(`categoryLarge`), 주요 태그(`sidebarTag`)가 별개 상태로 동작하면서, 사이드바 항목 클릭 시 기존 태그/드롭다운과 불필요한 교집합(AND) 연산이 수행됨.
  - 이로 인해 사이드바에 표시된 수량(예: `소프트웨어 627건`)과 실제 조회 결과(예: 교집합 348건)가 불일치하는 현상 발생.
- **개선 조치**:
  - **필터 상태 통합**: 사이드바 카테고리와 드롭다운 카테고리를 `activeFilter.categoryLarge` 단일 필터로 일원화.
  - **사이드바 대분류 클릭 연동**: 사이드바에서 `소프트웨어 (627)` 클릭 시, 잔여 태그/중분류 상태를 클리어하고 상단 `[대분류 선택]` 드롭다운을 `소프트웨어`로 자동 동기화하여 **정확히 627건이 100% 표출**되도록 수정.
  - **동적 중분류 옵션 동기화**: 대분류 선택 시 `[중분류 선택]` 드롭다운 옵션이 해당 대분류에 속하는 중분류 항목으로만 동적 갱신되도록 개선.
  - **사이드바 태그 클릭 연동**: 사이드바에서 `🏷️ OS (353)` 클릭 시 대분류 조건과 충돌하지 않도록 태그 필터를 전면 활성화하여 **정확히 353건이 100% 표출**되도록 수정.

---

## v1.5.0.Build.1 (2026-08-07 14:32)

### 🚀 Microsoft 공식 Windows 11 전체 228개 블루스크린(BSOD) Stop Code 자동 수집 & 전수 지식 인입 (총 1,742건)
- 228개 MS 공식 BSOD 지식 + 1,514개 사내 유지보수 데이터 = **총 1,742건 전수 탑재**
