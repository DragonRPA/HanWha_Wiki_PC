# 릴리즈 노트 (RELEASE NOTES)

## v1.8.1.Build.1 (2026-08-07 16:12)

### 🎨 화면 하단 이탈 방지 레이아웃 격리 & 15개씩 보기 (화면 맞춤) 대개편
- **화면 하단 짤림 원인 분석 및 해결**:
  - 기존 높이 오차 및 `min-height: 0` 누락으로 인해, 25개씩 보기 설정 시 노트북 화면 하단(작업표시줄 근처) 밖으로 테이블 최하단 행들이 잘려 나가는 현상 픽스.
  - `.app-container` 100vh 오프셋 정밀화(`calc(100vh - 52px)`), `.app-main` 및 `.table-container`에 `min-height: 0`을 적용하여 화면 이탈 없이 영역 내부에서 고정 스크롤되도록 구조 격리.
- **기본 페이지 보기 옵션 최적화 (`15개씩 보기 (화면 맞춤)`)**:
  - 기본 출력 개수를 노트북/모니터 화면 크기에 가장 정합한 **`15개씩 보기 (화면 맞춤)`**으로 변경.
  - 드롭다운 옵션에 `15개`, `20개`, `25개`, `50개`, `100개` 옵션 제공.
  - 테이블 행 간격(Padding 5px) 고밀도 미세조정을 통해 어떤 해상도에서도 화면 밖 짤림 없는 쾌적한 렌더링 보장.

---

## v1.8.0.Build.1 (2026-08-07 15:08)

### 📄 SSD 저장장치 불량 및 보안 통제 위키 `wiki-ssd-troubleshooting-v1.md` 작성 및 시드 인입 (총 1,760건)
- 5대 주요 SSD 트러블슈팅 KCS SBAR 정형화 시드 통합.
